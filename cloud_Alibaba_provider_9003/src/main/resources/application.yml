server:
  port: 9003
spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
#暴露所有端点
management:
  endpoints:
    web:
      exposure:
        include: '*'